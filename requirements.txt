# Pipeline dependencies (the Space has its own app/requirements.txt)
pandas>=2.2
pyarrow>=15
numpy>=1.26
pyyaml>=6
scikit-learn>=1.4
lightgbm>=4.3
plotly>=5.20
gradio==6.28.0
huggingface_hub>=0.23
pytest>=8
# Optional second model family, install when that work starts
# chronos-forecasting
# torch
